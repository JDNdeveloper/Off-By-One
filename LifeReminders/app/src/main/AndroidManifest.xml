<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jdndeveloper.lifereminders" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/lifereminders_app"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".AlarmRecieverAndService.AlarmReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".AlarmRecieverAndService.AlarmService"
            android:enabled="true" />

        <activity
            android:name=".SprintOnePresentation"
            android:label="@string/title_activity_sprint_one_presentation" >
        </activity>
    </application>

</manifest>
